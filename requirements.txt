# 核心依賴
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28

# Web 框架
flask>=2.3.0
flask-cors>=4.0.0

# 數據處理和分析
scipy>=1.10.0
scikit-learn>=1.3.0

# 技術指標計算
ta>=0.11.0
TA-Lib; sys_platform != 'win32'  # Linux/Mac 使用 TA-Lib

# 總體經濟分析（v4.0 新增）
requests>=2.31.0

# AI 輿情分析（v4.0 新增，可選）
newsapi-python>=0.2.7
vaderSentiment>=3.3.2

# 台股資料（可選，用於 FinMind 替代方案）
# FinMind>=1.3.0  # 有 Token 限制，建議使用 TWSE API

# 其他工具
python-dateutil>=2.8.0
pytz>=2023.0
